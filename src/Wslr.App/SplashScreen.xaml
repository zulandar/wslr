<Window x:Class="Wslr.App.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WSLR"
        Width="400" Height="280"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- Pulsing glow animation -->
        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="LogoGlow"
                             Storyboard.TargetProperty="Opacity"
                             From="0.3" To="0.8" Duration="0:0:1"
                             AutoReverse="True">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="LogoGlow"
                             Storyboard.TargetProperty="(UIElement.Effect).(BlurEffect.Radius)"
                             From="20" To="35" Duration="0:0:1"
                             AutoReverse="True">
                <DoubleAnimation.EasingFunction>
                    <SineEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Rotating ring animation -->
        <Storyboard x:Key="SpinAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="SpinnerRotation"
                             Storyboard.TargetProperty="Angle"
                             From="0" To="360" Duration="0:0:1.5"/>
        </Storyboard>

        <!-- Fade in animation -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetName="MainContent"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Fade out animation -->
        <Storyboard x:Key="FadeOutAnimation" Completed="FadeOutAnimation_Completed">
            <DoubleAnimation Storyboard.TargetName="MainContent"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <!-- Main container with rounded corners and shadow -->
    <Border x:Name="MainContent"
            CornerRadius="16"
            Opacity="0"
            Background="#1A1A1A">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>

        <Grid Margin="40,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo Section -->
            <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,30">
                <!-- Glow effect behind logo -->
                <Ellipse x:Name="LogoGlow"
                         Width="100" Height="100"
                         Opacity="0.5">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="#0078D4" Offset="0"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                    <Ellipse.Effect>
                        <BlurEffect Radius="25"/>
                    </Ellipse.Effect>
                </Ellipse>

                <!-- Logo circle -->
                <Ellipse Width="72" Height="72">
                    <Ellipse.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#0078D4" Offset="0"/>
                            <GradientStop Color="#005A9E" Offset="1"/>
                        </LinearGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>

                <!-- W letter -->
                <TextBlock Text="W"
                           FontFamily="Segoe UI"
                           FontSize="36"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- Title Section -->
            <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock Text="WSLR"
                           FontFamily="Segoe UI"
                           FontSize="32"
                           FontWeight="SemiBold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="WSL Instance Manager"
                           FontFamily="Segoe UI"
                           FontSize="14"
                           Foreground="#9CA3AF"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Loading Section -->
            <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,20,0,0">
                <!-- Spinning loader -->
                <Canvas Width="24" Height="24" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <Canvas.RenderTransform>
                        <RotateTransform x:Name="SpinnerRotation" CenterX="12" CenterY="12" Angle="0"/>
                    </Canvas.RenderTransform>

                    <!-- Spinner dots -->
                    <Ellipse Width="4" Height="4" Fill="#0078D4" Canvas.Left="10" Canvas.Top="0"/>
                    <Ellipse Width="4" Height="4" Fill="#0078D4" Opacity="0.85" Canvas.Left="17" Canvas.Top="3"/>
                    <Ellipse Width="4" Height="4" Fill="#0078D4" Opacity="0.7" Canvas.Left="20" Canvas.Top="10"/>
                    <Ellipse Width="4" Height="4" Fill="#0078D4" Opacity="0.55" Canvas.Left="17" Canvas.Top="17"/>
                    <Ellipse Width="4" Height="4" Fill="#0078D4" Opacity="0.4" Canvas.Left="10" Canvas.Top="20"/>
                    <Ellipse Width="4" Height="4" Fill="#0078D4" Opacity="0.3" Canvas.Left="3" Canvas.Top="17"/>
                    <Ellipse Width="4" Height="4" Fill="#0078D4" Opacity="0.2" Canvas.Left="0" Canvas.Top="10"/>
                    <Ellipse Width="4" Height="4" Fill="#0078D4" Opacity="0.1" Canvas.Left="3" Canvas.Top="3"/>
                </Canvas>

                <!-- Status text -->
                <TextBlock x:Name="StatusText"
                           Text="Starting..."
                           FontFamily="Segoe UI"
                           FontSize="12"
                           Foreground="#6B7280"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
